---
description: "Применять при ревью любого плана работ на соответствие правилам @common-plan-generator.mdc. АВТОМАТИЧЕСКИ применяется при: открытии файлов плана (*.md в PLAN/), запросах на ревью, обнаружении нарушений структуры. Ключевые слова для активации: 'ревью плана', 'проверка плана', 'соответствие плана', 'анализ структуры', 'план исправления', 'валидация плана', 'review plan', 'check plan', 'проверим план', 'план соответствует', 'нарушения плана', 'исправить план', 'структура неправильная'."
alwaysApply: false
---
# Common Plan Reviewer

Применяется для диагностики нарушений структуры планов на соответствие `@common-plan-generator.mdc` и `@catalogization-rules.mdc`.

## 🚨 ПЕРВЫЙ ОБЯЗАТЕЛЬНЫЙ ШАГ: АВТОМАТИЧЕСКАЯ ВАЛИДАЦИЯ

### ⚡ НЕМЕДЛЕННО запустить скрипт:
```powershell
PowerShell -ExecutionPolicy Bypass -File ".cursor/tools/PlanStructureValidator.ps1"
```

**🔥 КРИТИЧНО**: НЕ делать ничего другого до получения результатов валидации!
- НЕ читать файлы вручную
- НЕ анализировать структуру визуально  
- НЕ проверять ссылки руками
- **ТОЛЬКО автоматизированная валидация ПЕРВЫМ ДЕЛОМ!**

## 🚨 КРИТИЧЕСКАЯ ЗАЩИТА ПЛАНА РЕВЬЮ

**⚡ АРХИТЕКТОР НЕ ДОЛЖЕН ТРОГАТЬ ПЛАН РЕВЬЮ:**
- ❌ **АБСОЛЮТНО ЗАПРЕЩЕНО архитектору изменять статусы в плане ревью**
- ❌ **АРХИТЕКТОР НЕ МОЖЕТ устанавливать APPROVED/IN_PROGRESS/REQUIRES_VALIDATION**
- ❌ **ТОЛЬКО РЕВЬЮЕР имеет право изменять статусы файлов**
- ✅ **Архитектор ТОЛЬКО исправляет файлы плана, НЕ ТРОГАЕТ план ревью**

## 🎯 TODO В ПЛАНАХ = ОТЛИЧНО

**⚡ TODO-маркеры в планах НИКОГДА НЕ являются проблемой:**
- ✅ **TODO в планах = ХОРОШО** - это правильные планировочные индикаторы
- ✅ **TODO показывают архитектуру без реализации** - это цель планов
- ❌ **ЗАПРЕЩЕНО критиковать TODO в планах** - они соответствуют правилам

**✅ ПРАВИЛЬНЫЕ ФОРМУЛИРОВКИ:**
- "TODO корректно показывают планируемую реализацию"
- "TODO-маркеры соответствуют требованиям к планам"

## 🔥 КАТАЛОГИЗАЦИОННЫЕ ПРАВИЛА

**ВАЖНО**: Все правила каталогизации определены в `@catalogization-rules.mdc`. Скрипт проверяет соответствие ЗОЛОТЫМ ПРАВИЛАМ:
- **Правило #1**: Каталог называется идентично файлу плана (без .md)
- **Правило #2**: Координаторы находятся СНАРУЖИ своих каталогов

### ⚠️ После получения результатов скрипта:
Если скрипт показал нарушения → применить `@systematic-review.mdc` для исправления
Если скрипт показал 0 нарушений → продолжить с быстрой диагностикой ниже

### 🏗️ Архитектурная документация:
**ОБЯЗАТЕЛЬНО проверить**: Есть ли соответствующая архитектурная документация в `Docs/Architecture/Planned/`
- Если плана касается архитектуры → применить `@architecture-documenter.mdc`
- Проверить ссылки на архитектурную документацию в плане

---

## Диагностические проверки (ТОЛЬКО после автоматической валидации!)

### 🔍 Структурные проверки:
1. **Каталогизация**: См. полные критерии в `@catalogization-rules.mdc`
2. **LLM READINESS**: См. полный алгоритм в `@llm-readiness-assessment.mdc`
3. **Файлы в корне с родительскими ссылками на некорневые файлы** → критическое нарушение
4. **Многоуровневые файлы в корне** (`XX-YY-name.md`) → должны быть в каталогах
5. **Файлы >400 строк** → превышение технического лимита
6. **EXECUTION COMPLEXITY** → превышение лимита 20-30 tool calls на ЗАДАЧУ (### X.Y)
7. **ТЕХНИЧЕСКАЯ ИСПОЛНИМОСТЬ** → задачи должны содержать ВСЕ шаги для работающего результата

### 🎯 КРИТЕРИЙ ИСПОЛНИМОСТИ (20-30 TOOL CALLS НА ЗАДАЧУ)

**⚡ КЛЮЧЕВОЙ ПРИНЦИП: КАЖДАЯ ЗАДАЧА (### X.Y) ≤ 30 TOOL CALLS**

**ВАЖНО**: Лимит применяется к **отдельной задаче (секции ### X.Y)**, НЕ к целому файлу!
- Файл может содержать много задач, если каждая ≤ 30 tool calls
- Каждая секция `### X.Y` = отдельная задача для исполнения
- Подзадачи в дочерних файлах исполняются отдельно и оцениваются самостоятельно

**🔍 МЕТОДИКА АНАЛИЗА ЗАДАЧ:**
1. **Найти все секции `### X.Y`** в файле (это отдельные задачи)
2. **Проанализировать содержимое КАЖДОЙ секции**:
   - TODO items и checklist'ы
   - Code blocks и технические спецификации
   - API calls и database operations
   - Testing scenarios и validation steps
3. **Оценить complexity каждой задачи**:
   - Простая задача: 5-15 tool calls
   - Средняя задача: 15-25 tool calls
   - Сложная задача: 25-30 tool calls
   - **ПРЕВЫШЕНИЕ**: >30 tool calls → требует декомпозиции

**❌ ПРИЗНАКИ НАРУШЕНИЯ ЗАДАЧИ:**
- Задача содержит >10 TODO items
- Смешение разных типов операций (API + UI + Database в одной задаче)
- Требует >30 different tool calls для завершения
- Слишком много технических деталей в одной секции

**✅ ПРАВИЛЬНАЯ ДЕКОМПОЗИЦИЯ ЗАДАЧ:**
```
❌ ПЛОХО - одна задача (45 tool calls):
### 3.1 Полная миграция Backend
- [ ] Identity setup (15 tool calls)
- [ ] API controllers (15 tool calls)
- [ ] Database migration (15 tool calls)

✅ ХОРОШО - три задачи (по 15 tool calls):
### 3.1 Identity Migration (15 tool calls)
### 3.2 API Controllers Setup (15 tool calls)
### 3.3 Database Migration (15 tool calls)
```

**🔧 ИНСТРУМЕНТЫ ПРОВЕРКИ:**
1. **Автоматическая валидация**: `PowerShell -ExecutionPolicy Bypass -File ".cursor/tools/PlanStructureValidator.ps1"`
2. **Ручной анализ**: Если скрипт показывает превышения
3. **Task complexity estimation**: Подсчёт technical operations в каждой ### X.Y секции

### 🔧 КРИТЕРИЙ ТЕХНИЧЕСКОЙ ИСПОЛНИМОСТИ

**⚡ КЛЮЧЕВОЙ ПРИНЦИП: Каждая задача должна содержать ВСЕ шаги для работающего результата**

**СМЫСЛОВАЯ ПОЛНОТА И ИСПОЛНИМОСТЬ - НА ПЕРВОМ МЕСТЕ!**

**🔍 ПРОВЕРКА ИНТЕГРАЦИОННОЙ ПОЛНОТЫ:**

**Entity/Model создание → ТРЕБУЕТ полной интеграции:**
- ✅ Entity класс создание
- ✅ DbContext registration (добавить DbSet)
- ✅ Entity configuration (OnModelCreating)
- ✅ Database migration создание
- ✅ Index configuration для performance

**Repository/Service создание → ТРЕБУЕТ DI регистрации:**
- ✅ Interface определение
- ✅ Implementation класс
- ✅ DI registration в Program.cs/Startup.cs
- ✅ Dependencies resolution проверка

**API endpoint создание → ТРЕБУЕТ middleware setup:**
- ✅ Controller method реализация
- ✅ Request/Response models
- ✅ Authorization/Authentication настройка
- ✅ Routing configuration
- ✅ Error handling и validation

**Database изменения → ТРЕБУЕТ migration workflow:**
- ✅ Entity/Schema изменения
- ✅ Migration script создание
- ✅ Database update procedure
- ✅ Rollback strategy

**❌ ПРИЗНАКИ НЕПОЛНОЙ ЗАДАЧИ:**

```
❌ ПЛОХО: "Создать RefreshToken model и repository"
❌ ПЛОХО: "Реализовать UserService"
❌ ПЛОХО: "Добавить API endpoint"
❌ ПЛОХО: "Настроить authentication"
```

**Проблемы:**
- НЕ указано что делать с DbContext
- НЕ описана DI registration
- НЕ упомянута database migration
- НЕ указаны зависимости и их настройка

**✅ ПРАВИЛЬНАЯ ДЕТАЛИЗАЦИЯ:**

```
✅ ХОРОШО: Декомпозиция на интеграционные шаги:

### 3.1A: Создать RefreshToken entity класс
- Создать Core/Models/RefreshToken.cs
- Определить properties и relationships

### 3.1B: Интегрировать с Entity Framework
- Добавить DbSet в AuthDbContext
- Настроить entity configuration в OnModelCreating
- Создать migration: dotnet ef migrations add AddRefreshTokenTable

### 3.1C: Создать repository layer
- Создать IRefreshTokenRepository interface
- Реализовать RefreshTokenRepository класс
- Зарегистрировать в DI: services.AddScoped<IRefreshTokenRepository>()

### 3.1D: Валидация интеграции
- Проверить компиляцию (0 ошибок)
- Запустить миграцию в dev environment
- Проверить DI resolution при startup
```

**🎯 ДЕТАЛИЗАЦИЯ ПО ТИПАМ ТЕХНИЧЕСКИХ ЗАДАЧ:**

**1. Backend Entity Implementation:**
```
ОБЯЗАТЕЛЬНЫЕ шаги для Entity задач:
- [ ] Entity класс с properties
- [ ] DbContext.DbSet добавление
- [ ] Entity configuration (indexes, constraints)
- [ ] Database migration создание и тест
- [ ] DI registration если нужен repository
```

**2. Service Layer Implementation:**
```
ОБЯЗАТЕЛЬНЫЕ шаги для Service задач:
- [ ] Interface определение с методами
- [ ] Service implementation с dependencies
- [ ] DI registration в Program.cs/Startup
- [ ] Dependencies availability проверка
- [ ] Error handling и logging
```

**3. API Controller Implementation:**
```
ОБЯЗАТЕЛЬНЫЕ шаги для API задач:
- [ ] Controller class с action methods
- [ ] Request/Response DTO models
- [ ] Authorization attributes настройка
- [ ] Validation logic и error responses
- [ ] Routing и middleware integration
```

**4. Security/Auth Implementation:**
```
ОБЯЗАТЕЛЬНЫЕ шаги для Auth задач:
- [ ] Authentication scheme configuration
- [ ] Middleware registration в pipeline
- [ ] CORS настройка для client origins
- [ ] Token validation и refresh logic
- [ ] Error handling и security logging
```

**🚨 КРИТИЧЕСКИЕ НАРУШЕНИЯ ИСПОЛНИМОСТИ:**

1. **Entity без DbContext integration** → код не скомпилируется
2. **Service без DI registration** → runtime DI exceptions
3. **API без middleware setup** → authentication failures
4. **Database changes без migration** → schema mismatch errors

**АВТОМАТИЧЕСКАЯ ПРОВЕРКА В ВАЛИДАТОРЕ:**

PlanStructureValidator.ps1 должен проверять:
- Entity tasks → наличие DbContext/migration шагов
- Service tasks → наличие DI registration шагов
- API tasks → наличие middleware/routing шагов
- DB tasks → наличие migration workflow шагов

### 🚨 ПРИНЦИП "ПЛАН ≠ РЕАЛИЗАЦИЯ":
**✅ ПРАВИЛЬНО для плана:**
- Сигнатуры методов с TODO комментариями
- Структуры классов с описанием назначения
- Архитектурные схемы и интерфейсы
- Заглушки реализаций (`throw new NotImplementedException()`)

**❌ НЕПРАВИЛЬНО для плана:**
- Полные реализации методов с бизнес-логикой
- Детальные алгоритмы и LINQ запросы
- Готовый продакшн код

### ✅ НЕ ЯВЛЯЕТСЯ НАРУШЕНИЕМ:
- **TODO-маркеры**: планировочные индикаторы (это ХОРОШО)
- **Отсутствие полной реализации**: планы содержат архитектуру, НЕ готовый код

## Алгоритм ревью

### ШАГ 1: Автоматическая валидация ⚡
```powershell
PowerShell -ExecutionPolicy Bypass -File ".cursor/tools/PlanStructureValidator.ps1"
```

**ОБЯЗАТЕЛЬНЫЕ ПРОВЕРКИ СКРИПТА:**
1. **ЗОЛОТЫЕ ПРАВИЛА каталогизации** (координаторы, именование)
2. **РАЗМЕР ФАЙЛОВ** (≤400 строк)
3. **EXECUTION COMPLEXITY** (≤30 tool calls на задачу)
4. **🔧 ТЕХНИЧЕСКАЯ ИСПОЛНИМОСТЬ** (полнота интеграционных шагов)
5. **ССЫЛОЧНАЯ ЦЕЛОСТНОСТЬ** (корректные parent/child ссылки)

### ШАГ 2: Приоритизация нарушений по критичности
**🚨 КРИТИЧЕСКИЕ (блокируют исполнение):**
- ТЕХНИЧЕСКАЯ ИСПОЛНИМОСТЬ - задачи без интеграционных шагов
- EXECUTION COMPLEXITY >30 tool calls

**⚠️ ВАЖНЫЕ (влияют на качество):**
- ЗОЛОТЫЕ ПРАВИЛА каталогизации
- Размер файлов >400 строк

**💡 РЕКОМЕНДАЦИИ (улучшения):**
- Битые ссылки
- Пустые каталоги

### ШАГ 3: Анализ результатов и стратегия исправления

**Если найдены КРИТИЧЕСКИЕ нарушения → НЕМЕДЛЕННЫЕ ИСПРАВЛЕНИЯ:**
1. **ТЕХНИЧЕСКАЯ ИСПОЛНИМОСТЬ**: Применить шаблоны декомпозиции из `@common-plan-generator.mdc`
2. **EXECUTION COMPLEXITY**: Разбить сложные задачи (>30 tool calls) на подзадачи

**Если найдены ВАЖНЫЕ нарушения → Структурные исправления:**
1. **ЗОЛОТЫЕ ПРАВИЛА**: Применить `@catalogization-rules.mdc`
2. **Размер файлов**: Декомпозировать большие файлы

**Если 0 критических нарушений → Продолжить с ШАГ 4**

### ШАГ 4: Дополнительная диагностика качества (только если критических нарушений нет!)
1. **Структурный анализ**: Соответствие `@catalogization-rules.mdc`
2. **Обратные ссылки**: каждый дочерний файл содержит `**Родительский план**:`
3. **Проверка принципа "План ≠ Реализация"** (архитектурные заглушки vs полная реализация)
4. **Архитектурная документация**: соответствие в `Docs/Architecture/Planned/`

### ШАГ 5: Исправления по приоритету

**Приоритет 1: КРИТИЧЕСКИЕ (ОБЯЗАТЕЛЬНО исправить)**
- ТЕХНИЧЕСКАЯ ИСПОЛНИМОСТЬ → декомпозиция задач на интеграционные шаги
- EXECUTION COMPLEXITY → разбиение сложных задач

**Приоритет 2: ВАЖНЫЕ (рекомендуется исправить)**
- Структурные нарушения → применить `@common-plan-generator.mdc`
- Каталогизация → применить `@catalogization-rules.mdc`

**Приоритет 3: УЛУЧШЕНИЯ (при необходимости)**
- Битые ссылки → создать недостающие файлы или исправить пути
- Документация → дополнить архитектурные описания

### ШАГ 6: Automated Validation Support

**🔧 ДОПОЛНЕНИЕ К РУЧНОМУ РЕВЬЮ: Автоматизированная валидация готовности к LLM-исполнению**

**Агент**: plan-readiness-validator (`.cursor/agents/plan-readiness-validator/`)

**Цель**: Дополнить ручное структурное ревью количественной оценкой готовности плана к исполнению LLM-агентами.

**Когда использовать автоматизированную валидацию**:
1. **После milestone reviews** - когда завершена значимая часть плана
2. **После post-revision reviews** - после исправления структурных нарушений
3. **Перед final approval** - финальная проверка перед передачей на исполнение

**Интеграция с ручным ревью**:
- **Ручное ревью** (common-plan-reviewer) - фокус на структурных правилах, каталогизации, ЗОЛОТЫХ ПРАВИЛАХ
- **Автоматизированная валидация** (plan-readiness-validator) - фокус на готовности к LLM-исполнению, технической полноте, сложности задач
- **Комбинированный подход**: Ручное ревью ПЕРВИЧНО, автоматизированная валидация ДОПОЛНЯЕТ его

**Интерпретация оценок валидатора**:

```markdown
Оценка ≥90% (READY):
  → План технически готов к исполнению
  → Можно переходить к plan-task-executor
  → Рекомендация: Финализировать план

Оценка 80-89% (BORDERLINE):
  → Требуется дополнительное ручное ревью
  → Проверить критичные проблемы из отчета валидатора
  → Рекомендация: Исправить технические недостатки перед финализацией

Оценка <80% (REQUIRES_IMPROVEMENT):
  → Критичные проблемы в технической декомпозиции
  → План НЕ готов к исполнению без исправлений
  → Рекомендация: Применить @common-plan-generator.mdc для исправления
```

**Timing валидации в ревью процессе**:

```markdown
ЭТАП РЕВЬЮ:
1. Запустить PlanStructureValidator.ps1 (автоматическая структурная проверка)
2. Провести ручное ревью структуры по common-plan-reviewer.mdc
3. Исправить структурные нарушения (если найдены)

↓ ПОСЛЕ ЗАВЕРШЕНИЯ РУЧНОГО РЕВЬЮ ↓

4. Вызвать plan-readiness-validator для LLM readiness scoring
5. Проанализировать отчет валидатора
6. Если оценка <90%, исправить технические недостатки
7. Финальное approve только если:
   - Структурное ревью passed ✅
   - LLM readiness score ≥90% ✅
```

**Обработка расхождений между ручным и автоматизированным ревью**:

**Приоритет конфликтов**:
- **Ручное ревью имеет приоритет** над автоматизированным
- **Автоматизированная валидация** предоставляет количественный baseline

**Сценарии расхождений**:

```markdown
Сценарий 1: Ручное ревью PASSED, автоматизированная оценка <90%
→ Приоритет: Ручное ревью
→ Действие: Проанализировать причины низкой оценки
→ Решение: Если проблемы некритичны (напр. форматирование), финализировать план
          Если проблемы критичны (напр. missing integration steps), исправить

Сценарий 2: Ручное ревью REQUIRES_REVISION, автоматизированная оценка ≥90%
→ Приоритет: Ручное ревью
→ Действие: Сначала исправить структурные нарушения
→ Решение: Повторить автоматизированную валидацию ПОСЛЕ исправлений

Сценарий 3: Оба passed
→ Решение: План готов к финализации и исполнению ✅

Сценарий 4: Оба требуют улучшений
→ Решение: Сначала исправить структуру, затем технические недостатки
```

**Пример workflow с автоматизированной валидацией**:

```markdown
ШАГИ РЕВЬЮ:

1. Запустить автоматическую структурную проверку:
   PowerShell -ExecutionPolicy Bypass -File ".cursor/tools/PlanStructureValidator.ps1"

2. Провести ручное ревью по common-plan-reviewer.mdc
   - Проверить ЗОЛОТЫЕ ПРАВИЛА
   - Проверить техническую исполнимость
   - Проверить размеры файлов

3. Если структурные нарушения → Исправить → Повторить шаги 1-2

4. После PASSED ручного ревью → Автоматизированная валидация:

   Вызов через Task tool:
   subagent_type: "plan-readiness-validator"
   parameters:
     plan_file: "[путь к плану]"
     validation_type: "milestone_review"

5. Анализ отчета валидатора:

   IF score ≥90%:
     → План APPROVED для исполнения
     → Рекомендовать plan-task-executor

   ELSE IF score 80-89%:
     → Проверить критичные issues
     → Исправить технические недостатки
     → Повторить валидацию (max 3 iterations)

   ELSE (score <80%):
     → REQUIRES_REVISION
     → Применить @common-plan-generator.mdc
     → Исправить все критичные проблемы

6. Финальное решение:
   ✅ APPROVED - если ручное ревью PASSED И оценка ≥90%
   ⚠️ CONDITIONAL APPROVAL - если ручное ревью PASSED И оценка 80-89% (с документированием рисков)
   ❌ REQUIRES_REVISION - во всех других случаях
```

**ВАЖНО**: Автоматизированная валидация НЕ заменяет ручное ревью, а ДОПОЛНЯЕТ его. Ручной ревьюер всегда имеет финальное слово в принятии решения.

## Результат ревью

**При КРИТИЧЕСКИХ нарушениях**: 🚨 КРИТИЧЕСКИЕ НАРУШЕНИЯ → применить `@common-plan-generator.mdc` для исправления
- ТЕХНИЧЕСКАЯ ИСПОЛНИМОСТЬ → декомпозировать задачи на интеграционные шаги
- EXECUTION COMPLEXITY → разбить сложные задачи

**При ВАЖНЫХ нарушениях**: ⚠️ СТРУКТУРНЫЕ ПРОБЛЕМЫ → применить исправления
- ЗОЛОТЫЕ ПРАВИЛА → применить `@catalogization-rules.mdc`
- Размер файлов → декомпозировать большие файлы

**При соответствии**: ✅ ПЛАН СООТВЕТСТВУЕТ ПРАВИЛАМ → цикл завершен, план готов к исполнению

## Особые случаи

- **Миграция планов**: НЕ удалять файлы → создать каталоги → переместить → обновить ссылки
- **Большие планы** (>100 файлов): поэтапная каталогизация с валидацией ссылок
- **Дубли файлов** (`-Updated`, `-New`): консолидировать после анализа

## Автоматические триггеры

Применяется при:
- Открытии файлов `**/PLAN/**/*.md` 
- Обнаружении >20 файлов `.md` в корне плана
- Запросах "ревью плана", "проверка плана"
- Файлах с паттерном `XX-YY-name.md` в корне

## Итеративный цикл качества

**ЦИКЛИЧЕСКИЙ WORKFLOW до консенсуса:**
1. **Ревью** → `@common-plan-reviewer.mdc`
2. **Если нарушения** → `@common-plan-generator.mdc` для исправления
3. **Повторить** → до получения ✅ ПЛАН СООТВЕТСТВУЕТ ПРАВИЛАМ
4. **После консенсуса** → `@common-plan-executor.mdc` для исполнения

**Критерии завершения:**
- ✅ Структура соответствует `@catalogization-rules.mdc`
- ✅ Файлы ≤400 строк  
- ✅ Принцип "План ≠ Реализация" соблюден
- ✅ Нет критических нарушений

## Исправление нарушений

### Типичные исправления:
1. **Структурные нарушения**: См. инструкции в `@catalogization-rules.mdc`
2. **Файлы >400 строк**: Декомпозировать по логическим разделам
3. **Реализации в планах**: Заменить на TODO + архитектурные заглушки
   ```csharp
   // БЫЛО: полная реализация
   public bool IsValid() { return !string.IsNullOrWhiteSpace(Field); }
   
   // ДОЛЖНО БЫТЬ: архитектурная заглушка
   // TODO: Implement validation logic
   public bool IsValid() => throw new NotImplementedException();
   ```

**После исправлений → снова применить `@common-plan-reviewer.mdc`**